// Auto-generated by flatbed — do not edit.

import * as flatbuffers from "flatbuffers";
import type { EchoRequest, EchoResponse, GreetRequest, GreetResponse, Tag } from "./types.js";

export function encodeEchoRequest(builder: flatbuffers.Builder, value: EchoRequest): flatbuffers.Offset {
  const messageOffset = value.message != null ? builder.createString(value.message) : 0;
  const tagsOffset = value.tags != null
    ? (() => {
        const o = value.tags.map((x) => encodeTag(builder, x));
        builder.startVector(4, o.length, 4);
        [...o].reverse().forEach((off) => builder.addOffset(off));
        return builder.endVector();
      })()
    : 0;
  builder.startObject(4);
  if (messageOffset) builder.addFieldOffset(0, messageOffset, 0);
  builder.addFieldInt32(1, value.times, 0);
  builder.addFieldInt8(2, value.priority, 0);
  if (tagsOffset) builder.addFieldOffset(3, tagsOffset, 0);
  return builder.endObject();
}

export function encodeEchoRequestRoot(value: EchoRequest): Uint8Array {
  const builder = new flatbuffers.Builder();
  builder.finish(encodeEchoRequest(builder, value));
  return builder.asUint8Array();
}

export function decodeEchoResponse(bb: flatbuffers.ByteBuffer, pos: number): EchoResponse {
  const message_o = bb.__offset(pos, 4);
  return {
    message: message_o ? (bb.__string(pos + message_o) as string) : undefined,
  };
}

export function decodeEchoResponseRoot(bytes: Uint8Array): EchoResponse {
  const bb = new flatbuffers.ByteBuffer(bytes);
  return decodeEchoResponse(bb, bb.__indirect(bb.position()));
}

export function encodeGreetRequest(builder: flatbuffers.Builder, value: GreetRequest): flatbuffers.Offset {
  const nameOffset = value.name != null ? builder.createString(value.name) : 0;
  builder.startObject(1);
  if (nameOffset) builder.addFieldOffset(0, nameOffset, 0);
  return builder.endObject();
}

export function encodeGreetRequestRoot(value: GreetRequest): Uint8Array {
  const builder = new flatbuffers.Builder();
  builder.finish(encodeGreetRequest(builder, value));
  return builder.asUint8Array();
}

export function decodeGreetResponse(bb: flatbuffers.ByteBuffer, pos: number): GreetResponse {
  const greeting_o = bb.__offset(pos, 4);
  return {
    greeting: greeting_o ? (bb.__string(pos + greeting_o) as string) : undefined,
  };
}

export function decodeGreetResponseRoot(bytes: Uint8Array): GreetResponse {
  const bb = new flatbuffers.ByteBuffer(bytes);
  return decodeGreetResponse(bb, bb.__indirect(bb.position()));
}

export function encodeTag(builder: flatbuffers.Builder, value: Tag): flatbuffers.Offset {
  const labelOffset = value.label != null ? builder.createString(value.label) : 0;
  builder.startObject(1);
  if (labelOffset) builder.addFieldOffset(0, labelOffset, 0);
  return builder.endObject();
}

