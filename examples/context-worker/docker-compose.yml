services:
  app:
    build:
      # Repo root, so the ../../crates/flatbed path dependency resolves.
      context: ../..
      dockerfile: examples/context-worker/Dockerfile
    ports:
      - "8080:8080"
