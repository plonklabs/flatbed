services:
  app:
    build:
      # Repo root, so the ../../crates/flatbed path dependency resolves.
      context: ../..
      dockerfile: examples/static-assets/Dockerfile
    ports:
      - "8080:8080"
