/target
# Each example under examples/ is its own standalone Cargo package.
examples/*/target/
examples/*/Cargo.lock
**/*.rs.bk
**/*.bak
Cargo.lock.bak

node_modules/
clients/ts/**/dist/

# IDE noise
.idea/
.vscode/
*.swp
*.swo

# OS noise
.DS_Store
Thumbs.db

# Claude Code session artifacts that shouldn't propagate
.claude/worktrees/
.claude/settings.local.json

# Worker seats (parallel dev checkouts) and local fleet ledger
worktrees/flatbed*/
.plonk/local/

# Cached pinned toolchain binaries (flatc), shared across worktrees
.cache/flatc/

# Orchestrator identity (seeded from .env.tpl)
.env
