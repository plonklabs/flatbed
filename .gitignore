/target
# Each example under examples/ is its own standalone Cargo package.
examples/*/target/
examples/*/Cargo.lock
**/*.rs.bk
**/*.bak
Cargo.lock.bak

# npm workspace build/dependency output (any package under the workspace)
node_modules/
clients/ts/**/dist/

# IDE noise
.idea/
.vscode/
*.swp
*.swo

# OS noise
.DS_Store
Thumbs.db

# Claude Code session artifacts that shouldn't propagate
.claude/worktrees/
.claude/settings.local.json
